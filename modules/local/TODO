* Flesh out the testsuite

* Add a common base class to handle username variations
  * Auto-update the username variations hourly from Parrot's CREDITS file

* Trac converts "..." to a unicode ellipsis, which crashes parrotlog

* Commonize the trac plugin, it'll be useful eventually

* Add a common parser for bitbucket (hg), the shakespeare HLL uses it
