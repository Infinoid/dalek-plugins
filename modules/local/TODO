* Flesh out the testsuite, bring coverage up to 100%

* Trac converts "..." to a unicode ellipsis, which crashes parrotlog

* Commonize the trac plugin, make sure it autodetects the "parrot" and "languages" repos

* Add a common parser for bitbucket (hg), the shakespeare HLL uses it
